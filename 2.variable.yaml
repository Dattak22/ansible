- name: variable example
  hosts: all
  vars:
    URL: "cbz.google.com"  
  tasks: 
    - name: print url
      debug: 
        msg: "{{ URL }}"

    - name: print another url
      debug: 
        msg: "{{ URL }}"
- name: gathering facts
  hosts: all
  tasks:
      - name: print OS
        debug:
          msg: "{{ ansible_distribution }}"

      - name: print IP
        debug:
          msg: "{{ ipv4.addreansible_default_ipv4ss }}"
    
- name: gathering facts
  hosts: all
  tasks:
      - name: print OS
        debug:
          msg: "{{ ansible_distribution }}"

      - name: print IP
        debug:
          msg: "{{ ansible_default_ipv4. }}"
    